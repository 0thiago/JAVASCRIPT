//início loop simples - WHILE - estrutura de repetição com teste lógico no início

var contador = 1;

    while (contador <= 10) {
        console.log(`Tudo bem? WHILE ${contador}`);
        contador++
    }

//fim loop simples


//início loop simples - DO - estrutura de repetição com teste lógico no final

contador = 1;

do {
    console.log(`Tudo bem? DO ${contador}`);
    contador++
} while (contador <= 10);

//fim loop simples


